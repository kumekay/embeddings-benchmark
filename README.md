# Computation benchmark for embeddings models

## Usage

Run the benchmark with:

```bash
uv run python main.py
```

Output will be automatically saved to `log_benchmark.txt` and also displayed in the console.

To save output manually to a file:

```bash
uv run python main.py > output.txt
```
